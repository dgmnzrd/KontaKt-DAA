package com.kontakt.app.ui.theme

import androidx.compose.ui.graphics.Color

/* ---------- Colores principales (hex exactos de tu imagen) ---------- */
val Black         = Color(0xFF000000)   // Texto sobre backgrounds claros
val PayneGray     = Color(0xFF596475)   // Tertiary
val Copper        = Color(0xFFA8763E)   // Secondary
val CambridgeBlue = Color(0xFF729B79)   // Primary
val White         = Color(0xFFFFFFFF)

/* ---------- Variantes claras (≈ +40 % blanco) ---------- */
val PayneGrayLight     = Color(0xFF8FA0B2)   // 0x59→0x8F, 0x64→0xA0, 0x75→0xB2
val CopperLight        = Color(0xFFC99A7A)   // 0xA8→0xC9, 0x76→0x9A, 0x3E→0x7A
val CambridgeBlueLight = Color(0xFFAAC3AF)   // 0x72→0xAA, 0x9B→0xC3, 0x79→0xAF

/* ---------- Variantes oscuras (≈ –40 % blanco = +40 % negro) ---------- */
val PayneGrayDark     = Color(0xFF353C48)
val CopperDark        = Color(0xFF754E26)
val CambridgeBlueDark = Color(0xFF305B46)